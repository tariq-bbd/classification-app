using System.ComponentModel.DataAnnotations;

namespace ClassificationApp.Constants
{
    public enum Sex
    {
        [Display(Name = "Male")]
        MALE,
        [Display(Name = "Female")]
        FEMALE
    }
}
